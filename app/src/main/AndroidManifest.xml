<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="java">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="Color.activity.TopSelect"></activity>
        <activity android:name="Color.activity.tone_in_on"/>
        <activity android:name="Color.BottomSelect" />

        <activity
            android:name="Login_Main.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="Login_Main.activity.JoinActivity" />
        <activity android:name="Login_Main.activity.LoginActivity" />
        <activity android:name="Login_Main.activity.TestActivity" />

        <activity android:name="Cutout.CutOutActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
        <activity android:name="Cutout.IntroActivity"
            android:theme="@style/Base.Theme.AppCompat"
            android:label="@string/intro_label"/>
        <activity android:name="Cutout.CutOut_MainActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>